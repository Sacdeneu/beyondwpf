<UserControl x:Class="BeyondWPF.BaseApplication.Views.TextBoxPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <StackPanel>
                <TextBlock Text="TextBox Styles" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
                
                <TextBlock Text="States" FontSize="18" FontWeight="Medium" Margin="0,10,0,10" Foreground="{DynamicResource TextBrush}"/>
                
                <WrapPanel>
                    <!-- Default -->
                    <StackPanel Margin="10">
                        <TextBlock Text="Default" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
                        <TextBox Width="200" Text="Sample Text"/>
                    </StackPanel>

                    <!-- Placeholder/Watermark (needs support in style usually, putting text for now) -->
                    <StackPanel Margin="10">
                        <TextBlock Text="Read Only" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
                        <TextBox Width="200" Text="Read Only Text" IsReadOnly="True"/>
                    </StackPanel>

                    <!-- Disabled -->
                    <StackPanel Margin="10">
                        <TextBlock Text="Disabled" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
                        <TextBox Width="200" Text="Disabled Text" IsEnabled="False"/>
                    </StackPanel>
                </WrapPanel>

                <TextBlock Text="PasswordBox" FontSize="18" FontWeight="Medium" Margin="0,20,0,10" Foreground="{DynamicResource TextBrush}"/>
                 <WrapPanel>
                    <StackPanel Margin="10">
                        <TextBlock Text="Default" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
                        <PasswordBox Width="200"/>
                    </StackPanel>
                </WrapPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>

<UserControl x:Class="BeyondWPF.BaseApplication.Views.NotificationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BeyondWPF.BaseApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Native Notifications" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
            
            <TextBlock Text="Test the native Windows notification system. These toasts use the built-in Windows Runtime APIs." 
                       Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,10">
                <Button Content="Send Info" Click="SendInfo_Click" Width="120" Margin="0,0,10,0"/>
                <Button Content="Send Success" Click="SendSuccess_Click" Width="120" Margin="0,0,10,0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,10">
                <Button Content="Send Warning" Click="SendWarning_Click" Width="120" Margin="0,0,10,0"/>
                <Button Content="Send Error" Click="SendError_Click" Width="120" Margin="0,0,10,0"/>
            </StackPanel>

            <GroupBox Header="Custom Notification" Margin="0,20,0,0" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource BorderLightBrush}">
                <StackPanel Margin="10">
                    <TextBlock Text="Title:" Foreground="{DynamicResource TextBrush}" Margin="0,5"/>
                    <TextBox x:Name="TitleTextBox" Text="BeyondWPF Notification" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Message:" Foreground="{DynamicResource TextBrush}" Margin="0,5"/>
                    <TextBox x:Name="MessageTextBox" Text="Ceci est un message de test !" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>
                    
                    <Button Content="Send Custom Toast" Click="SendCustom_Click" HorizontalAlignment="Left" Width="150" Margin="0,10,0,0"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>

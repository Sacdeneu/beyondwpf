<UserControl x:Class="BeyondWPF.BaseApplication.Views.ViewModelsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:BeyondWPF.BaseApplication.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800"
             Background="Transparent">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="New ViewModel Types" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>

            <!-- Async Section -->
            <Border Background="{DynamicResource ControlDarkBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel DataContext="{Binding AsyncExample}">
                    <TextBlock Text="AsyncViewModel (Task Tracking)" FontSize="18" FontWeight="Medium" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="Tracks multiple background tasks and manages the IsBusy state automatically." 
                             Foreground="{DynamicResource TextBrush}" Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,15"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="Run 2s Task" Command="{Binding SimulateTaskCommand}" Margin="0,0,10,0" Width="120"/>
                        <Button Content="Run Multi Tasks" Command="{Binding SimulateMultipleTasksCommand}" Width="150"/>
                    </StackPanel>

                    <ProgressBar IsIndeterminate="True" Height="4" Margin="0,0,0,15"
                                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Local IsBusy State:" Foreground="{DynamicResource TextBrush}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="1" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="LightGreen"/>
                                    <Setter Property="Text" Value="IDLE"/>
                                    <Setter Property="Opacity" Value="0.5"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                            <Setter Property="Text" Value="WORKING..."/>
                                            <Setter Property="Opacity" Value="1"/>
                                            <Setter Property="FontWeight" Value="ExtraBold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Form Section -->
            <Border Background="{DynamicResource ControlDarkBrush}" CornerRadius="8" Padding="20">
                <StackPanel DataContext="{Binding FormExample}">
                    <TextBlock Text="FormViewModel (Validation)" FontSize="18" FontWeight="Medium" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="Uses DataAnnotations ([Required], [EmailAddress]) and ObservableValidator." 
                             Foreground="{DynamicResource TextBrush}" Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,15"/>

                    <TextBlock Text="Username (Min 3 chars)" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" x:Name="UserBox" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Email Address" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" x:Name="EmailBox" Margin="0,0,0,20"/>

                    <Button Content="Submit Form" Command="{Binding SubmitCommand}" HorizontalAlignment="Left" Width="150" Margin="0,0,0,10"/>

                    <TextBlock Text="Form is Valid!" Foreground="LightGreen" FontWeight="Bold" Margin="0,0,0,10">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasErrors}" Value="False">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <ItemsControl ItemsSource="{Binding (Validation.Errors), ElementName=UserBox}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ErrorContent}" Foreground="Red" FontSize="12" Margin="0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding (Validation.Errors), ElementName=EmailBox}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ErrorContent}" Foreground="Red" FontSize="12" Margin="0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    
                    <TextBlock Text="Has Errors:" Foreground="{DynamicResource TextBrush}" Margin="0,10,0,0"/>
                    <TextBlock Text="{Binding HasErrors}" FontWeight="Bold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                        <Setter Property="Foreground" Value="Red"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding HasErrors}" Value="False">
                                        <Setter Property="Foreground" Value="LightGreen"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

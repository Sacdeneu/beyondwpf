<UserControl x:Class="BeyondWPF.BaseApplication.Views.ContextMenuPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="ContextMenu" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,20"/>
            
            <TextBlock Text="Right-click the elements below to see the modern ContextMenu style." 
                       Foreground="{DynamicResource SecondaryBrush}" Margin="0,0,0,30"/>

            <UniformGrid Columns="2">
                <Border Background="{DynamicResource ControlDarkBrush}" CornerRadius="8" Padding="40" Margin="10" 
                        BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1">
                    <TextBlock Text="Right click me!" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    <Border.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="New File">
                                <MenuItem.Icon>
                                    <iconPacks:PackIconMaterial Kind="FilePlus" Width="14" Height="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Open Folder">
                                <MenuItem.Icon>
                                    <iconPacks:PackIconMaterial Kind="FolderOpen" Width="14" Height="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Edit" InputGestureText="Ctrl+E">
                                <MenuItem.Icon>
                                    <iconPacks:PackIconMaterial Kind="Pencil" Width="14" Height="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Delete" InputGestureText="Del">
                                <MenuItem.Icon>
                                    <iconPacks:PackIconMaterial Kind="Delete" Width="14" Height="14"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </Border.ContextMenu>
                </Border>

                <Button Content="Right Click Button" Height="60" Margin="10">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Option 1"/>
                            <MenuItem Header="Option 2">
                                <MenuItem Header="Sub-option 2.1"/>
                                <MenuItem Header="Sub-option 2.2"/>
                            </MenuItem>
                            <Separator/>
                            <MenuItem Header="Exit"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </UniformGrid>
        </StackPanel>
    </Grid>
</UserControl>
